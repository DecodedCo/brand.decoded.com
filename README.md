# brand-guidelines

## Development

To start a development server, run:
``` shell
npm run dev
```
and in a second window:
To start a development server, run:
``` shell
npm run start
```

To build all files and deploy, run:

``` shell
npm run build
```
